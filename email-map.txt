#!/usr/bin/env python
import sys

def callback(data):
    email = data.email
    if email == b'old@email.com':
        data.email = b'new@email.com'
    return data

# Don't forget to include this line at the end
__filter__ = {"commit": callback}